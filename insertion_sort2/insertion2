def insertion_sort2(L):
    for i in range(1, len(L)):
        insert2(L, i)
    return L


def insert2(L, i):
    value = L[i]
    while i > 0:
        if L[i - 1] > value:
            L[i] = L[i - 1]
            i -= 1
        else:
            L[i] = value
            return L